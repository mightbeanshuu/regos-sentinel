// Bridge to reuse the mature product primitives from the existing demo system.
// BrowserChrome is now the physics-driven SaaSBrowser (rise + glass + typed URL);
// it is a drop-in superset (same url/children/widthPct/maxWidth API).
export {SaaSBrowser as BrowserChrome} from './vector/SaaSBrowser';
export {UICursor} from '../demo/UICursor';
